## Phase 221 -- propagated the Phase-217 circular-rebus correction through active synthesis docs (2026-08-10)

A repository-wide consistency search found that the canonical creator clue
ledger and six older synthesis documents still displayed the retracted
`H`/initials-of-“your eyes”/`BUT` construction without a prominent correction.
This was capable of silently reintroducing the exact circular bridge removed
from code in Phase 217.

The creator ledger now retains only what survives mechanically: the
cross-source-stable forward-one words `BOTH/ULTIMATELY/THE`, their BUT/HYE
edges, and the bounded mirror9 fact `b <-> h` with `e` fixed. Its dependency
graph now ends at an unknown post-recognition operation. The non-blob,
matrixsumlist, yin-yang plan/transition/inventory, and bird-view documents
carry top-of-file correction notices so their historical searches remain
readable without being mistaken for current evidence.

`tools/gsmg/post_phase217_consistency_audit.py` regression-guards both sides:
the circular string cannot reappear in the active creator ledger, and every
legacy synthesis document containing the historical construction must retain
a Phase-217/circularity notice in its opening lines.

**Verdict:** BUT/HYE remains a robust recognition checkpoint and mirror9
supplies a limited `b/h`, fixed-`e` relation. Neither supplies a post-yinyang
operator. The repository's active evidence chain now says this consistently.

