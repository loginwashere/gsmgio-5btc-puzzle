## Phase 244 -- DBBI/FAED cross-capture stability: SalPhaseIon textarea byte-identical across all 5 known Wayback captures, G-ESC-001 page-boundary branch closed (2026-08-11)

**Question:** Phase 243 flagged one untried step: whether the byte-count
growth across the 5 known Wayback captures (4556 -> 4556 -> 4588 -> 5092 ->
5092) is a localized, stable difference inside the DBBI-matrixsumlist-FAED
region, or just archive boilerplate/global growth elsewhere on the page.
Pre-registered success condition (unchanged from Phase 243): a localized,
stable difference affecting that specific region -- not head-section
whitespace or third-party script churn.

**Frozen inputs:** the 3 Wayback captures whose raw bytes were not yet
locally available (`salphaseion_wayback_history_audit.CAPTURES[1:4]`,
timestamps `20231127181947`, `20241123015038`, `20251031153559`), fetched
exactly once via the same raw-capture route the existing Wayback-history
script already uses, plus the earliest capture (`20230601222752`) for a
complete 5-of-5 picture, plus the local mirror (`20260405154227`, already
on disk). This was a single bounded fetch, not an open-ended crawl: all 3
new captures were fetched once, verified, diffed, and the check stopped
regardless of outcome, per the pre-registered scope.

**Method:** `tools/gsmg/dbbi_faed_boundary_selector_audit.py --live` fetches
each capture's raw bytes, verifies each against the sha256/byte_count
already pinned in `salphaseion_wayback_history_audit.CAPTURES` (hard-fail on
mismatch -- integrity checked, not merely trusted), extracts the SalPhaseIon
`<textarea>`'s full text content from all 5 captures, and compares it
directly (not via a line-indexed diff, which a change in line count would
corrupt) alongside a whole-file line diff between each consecutive pair.

**Result:** all 3 fetched captures matched their pinned sha256/byte_count
exactly. The SalPhaseIon textarea's content -- DBBI, the binary-ASCII
`matrixsumlist` span, FAED, the `z`-separated decimal instructions, the hash
prefix/suffix, and both embedded AES blob halves -- is **byte-identical
across all 5 captures**, spanning 2023-06-01 to 2026-04-05 (nearly 3 years).
Every byte difference found across the 5 captures is archive boilerplate:
the already-closed `H1`/`h1` heading-case change (captures 1-2), `<head>`
whitespace/indentation reformatting (captures 2-3, +32 bytes), and a
third-party Cloudflare analytics `<script>` tag with a rotating version
token (captures 3-4, +504 bytes; captures 4-5, same-size token rotation
only). None of these touch the SalPhaseIon textarea. Pre-registered
condition: **not met**.

**Disposition:** rejected (for this specific branch of G-ESC-001).

**Facts affected:** extends `F-OBJ-003` in
[GSMG_FACT_LEDGER](../../../doc/GSMG_FACT_LEDGER.md).

**Supersedes/corrects:** none; completes the reopen-condition step Phase 243
explicitly left unfetched.

**Artifacts:** `tools/gsmg/dbbi_faed_boundary_selector_audit.py --live`;
full report:
[doc/GSMG_DBBI_FAED_BOUNDARY_SELECTOR_AUDIT.md](../../../doc/GSMG_DBBI_FAED_BOUNDARY_SELECTOR_AUDIT.md).

**Reopen condition:** this exhausts the last concretely actionable,
locally-executable step within G-ESC-001's page-boundary branch. The gap
now depends entirely on a genuinely external source -- a new creator
message, a primary artifact outside this page, or a Wayback capture beyond
the 5 already known and now fully diffed. `G-ESC-001`'s registry state moves
from `open` to `parked`; with this change, no P0 gap in
[GSMG_OPEN_GAP_REGISTRY](../../../doc/GSMG_OPEN_GAP_REGISTRY.md) remains both
`open` and locally actionable -- the frontier is honestly exhausted pending
new primary evidence.

