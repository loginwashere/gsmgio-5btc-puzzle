## Phase 401 -- BTCSEED/P91/Z brainstorm Priority 5: P91 against the YOUWON-bearing DBBI-M91 difference, closed negative (2026-08-25)

**Question:** Priority 5 of the 2026-08-25 BTCSEED/P91/Z continuation
brainstorm tests `P91` against `A26 = DBBI - M91 mod 26` -- Phase 75's
own already-known `YOUWON`-bearing difference -- through a small, frozen
ternary/modulo-5 family, on the grounds that this is more grounded than
inventing another arbitrary P91/DBBI operator.

**Method:** wrote
`tools/gsmg/phase401_p91z_priority5_youwon_difference_algebra_audit.py`
against a contract the user froze in full before any code was written:
3 alphabet-space candidates (`P91-A26`, `P91+A26`, `A26-P91`, mod 26) and
3 coordinate-space candidates (`coords(P91)-A5`, `coords(P91)+A5`,
`A5-coords(P91)`, mod 5, mapped back through Phase 386's DBBI-keyed
square, where `A5 = coords(DBBI)-coords(M91) mod 5`) -- 6 strings total,
no alternate squares, axis swaps, routes, offsets, or extra arithmetic.
Evaluated with Phase 396's frozen keyword list and Phase 387's own frozen
quadgram table (no dictionary-count ambiguity), calibrated against 100,000
P91-multiset-preserving shuffles (`A26`/`A5` held fixed, since they derive
from `DBBI`/`M91`, not `P91`), comparing family-maximum quadgram scores.
36 materials (6 outputs x 2 case forms x 3 hash forms) against all 4
blobs under the full oracle (17,280 effective attempts); 12 digests (6
outputs x 2 case forms, SHA-256 binary only) as direct secp256k1 scalars
against the exact prize address, compressed and uncompressed (24 address
checks) -- no BIP32 tree, per the contract's own exclusion. Self-tests
assert the mod-26 and coordinate-space inverse identities hold by
construction, a synthetic planted-English string trips the keyword
detector through the real `P91-A26` transform, a known scalar/address
pair fires the direct-key detector, and the real data's oracle/address
hit lists are asserted empty.

**Result:** both planted positives fire correctly. Real data: **zero**
keyword hits across all 6 candidates; family-maximum quadgram score
`-649.21`; **6,007/100,000 (6.01%)** multiset-preserving shuffles reach at
least that same family-maximum -- far above the 0.5% promotion bound.
Oracle: 17,280 effective attempts across 36 materials, **zero hits**.
Direct-key: 24 address checks, **zero hits**.

**Disposition:** closes negative per the contract's own promotion rule
(exact blob/address hit, or family-wise `p <= 0.005`; neither obtains).
The `YOUWON`-bearing difference does not extend to `P91` under this
frozen family.

**Artifacts:**
`tools/gsmg/phase401_p91z_priority5_youwon_difference_algebra_audit.py`;
permanent regression in `tools/gsmg/test_recent_audits.py`.

