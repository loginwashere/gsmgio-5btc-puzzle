## Phase 422 -- P32TRAILING macro-clue augmentation panel: closed `protocol_invalid`, inherited residue-filter contradiction (2026-08-26)

**Question:** when Phase 421's working, solution-complete sealed-target panel is
augmented with the authenticated eight-fragment creator macro clue and the
project's bounded macro-chain state, does the panel converge on a comparison-new
preimage that opens P32TRAILING?

**Method:** Phase 422 was preregistered and committed separately, then
implemented and frozen before execution. The 14,082-character packet (`sha256
ba01a36d...be834`) preserved Phase 418 byte-for-byte and added message 8446's
authenticated ordered fragments, the accurately labeled `574061 -> [23,16,7]
-> BUT/HYE` working grammar, parked `BYE`, unresolved `yinyang`/eyes transition,
and bounded `SalPhaseIon -> SalVATIon -> SALVATION` title rebus. Its 16,551-
character/257-line prompt (`sha256 f97756c6...8dc3`) was delivered through the
working 672-character escalated one-file launcher (`sha256 050ab622...b200`).
The frozen prior-evaluation comparator covered 1,972,858 exact byte strings:
Phase 270's 50 materials, Phase 421's eight actually evaluated promotions, and
1,972,800 regenerated Phase-322/334 macro-permutation material forms.

Five fresh `fork_turns: none` invocations received byte-identical launcher
bytes. All five returned the exact four-key schema, ten candidates, contiguous
ranks, `bootstrap_read_used: true`, and `other_tool_used: false`; independent
tool telemetry was unavailable. The responses contained 50 candidate
occurrences and 30 distinct display strings.

**Result:** zero submissions were eligible. The implementation had inherited
Phase 416's `blinding_violations()` filter unchanged. That filter forbids the
tokens `yinyang`, `SalPhaseIon`, and their `SALPH`/`SALPHASEION` substrings,
because those terms were absent from the original Phase 416 evidence packet.
Phase 422 deliberately made `yinyang` and `SalPhaseIon` solver-visible, but its
implementation failed to subtract newly disclosed macro terms from the
response-residue filter. Invocations 1-3 and 5 repeated `yinyang` in exact
candidates; invocation 4 repeated `SalPhaseIon` in its derivation. Each was
therefore mechanically rejected as `submission contains blinded-target
residue` despite following the new packet. With zero eligible responses after
five IDs, only three IDs remained under the eight-ID cap, so obtaining the five
required eligible submissions was mathematically impossible. IDs 6-8 were not
launched merely to prove that arithmetic.

**Branch:** `protocol_invalid` -- the solver-visible evidence and inherited
eligibility filter contradicted each other. There were zero promotions and zero
oracle calls; no candidate, vote, or puzzle inference carries forward.

**Interpretation:** this is an implementation/protocol failure, not evidence
for or against P32TRAILING or the macro clue. The delivery mechanism worked and
the solvers responded, but the eligibility wrapper did not adapt to the
preregistered evidence delta. No response may be repaired or reused. A retry
requires a newly numbered correction that derives the allowed response-residue
vocabulary from the exact solver-visible Phase 422 supplement while retaining
the original sealed-target terms, then freezes and tests that filter before any
new invocation. Phase 422 does not authorize that retry.

**Artifacts:** `phase422_frozen_launcher.txt`;
`phase422_p32trailing_macro_clue_panel_audit.py`;
`phase422_execution_replay.py`; `phase422_invocation_ledger.json`;
`phase422_result.json`.


