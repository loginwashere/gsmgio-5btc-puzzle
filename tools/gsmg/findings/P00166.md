## Phase 166 -- brainstorm item 4: pruning the reconstruction tree upstream instead of adding more leaf variants (2026-08-07)

**Provenance.** Item 4 of the same 2026-08-07 in-chat brainstorm as Phases
164-165: *"Prune the reconstruction tree higher up, not just at the
leaves. The current creator-ledger chain... is an intricate multi-year
solver-community construction, not something confirmed step-by-step by
the creator... When a leaf variant fails, it may be worth questioning an
upstream link instead of just trying more variants at the same depth."*
Unlike items 1-3, this isn't a code gap to close -- it's a question about
where this project's own compute has actually been going, checked against
[doc/GSMG_CREATOR_AUTHORED_CLUE_LEDGER.md](../../../doc/GSMG_CREATOR_AUTHORED_CLUE_LEDGER.md).

**Checked the two most plausible upstream "prune points" concretely.**

1. **The `574061` polarity choice** (blue=1/yellow=0 -> rose RGB vs.
   yellow=1/blue=0 -> prime `574061`) is the single most upstream,
   highest-leverage node in the whole chain -- if wrong, everything
   downstream is too. Re-read Phase 31's derivation: both polarities are
   kept and used for distinct creator-textual reasons (rose matches the
   creator's own "Roses…" line; the inverse polarity's prime matches
   `yellowblueprimes`), not one cherry-picked because it "looked more
   meaningful." Well-grounded; not a good prune target.

2. **The `BUT`/`HYE` rail -> mirror-alphabet escape-pair reading**
   (Phase 33/34: Architect-dialogue word-edges `BUT`/`HYE`, filtered to
   the puzzle's native `a-i` alphabet, treated as seeding a `{b,e}`/`{h,e}`
   checkerboard-cipher escape pair for `DBBI`/`FAED`) is where a real
   instance of exactly the pattern item 4 describes already happened --
   and the project already caught it. Phase 34 (2026-07-25) itself
   concluded *"`BUT/HYE` likely is the promised recognizable yin-yang
   state, not another literal password"* -- an early upstream caution.
   Substantial further leaf-level compute was spent on the same node
   anyway (Phase 43's monoalphabetic hill-climb, Phase 112's rank
   analysis, curated/exact-seed chain-addition and autokey backfills,
   the large-dictionary alphabet sweep) before Phase 124 (2026-07-28,
   `yinyang_artifact_inventory_audit.py`) ran a formal, pre-registered
   7-artifact qualification gate (primary provenance, visible before
   decryption, genuine dual structure, correct transition boundary, plus
   an independent discriminator) and confirmed: `BUT/HYE` is the *only*
   candidate artifact that passes every gate, but its complete bounded
   local cipher-family is exhausted with no deterministic downstream
   operation -- verdict *"no retained artifact currently satisfies both
   the full evidence qualification and a surviving deterministic
   downstream operation. No new cipher sweep follows."* This session's
   independent re-derivation of the same conclusion (via the "sloppy
   2-day build" prior rather than the formal gate audit) corroborates
   Phase 124 rather than surfacing something new -- worth recording as
   confirmation, not treated as a fresh finding.

**One concrete gap did remain and was closed cleanly: the literal rail
tokens themselves, isolated, under every current oracle.** Historical
coverage of `BUT`/`HYE`/`EOL`/`HEY`/`BUTHYE`/`HYEBUT` as literal
passphrase/key material was real (Phase 33's original 216-keystring probe;
Phase 79's full 648-curated-corpus run once `matrixsumlist_choice_
candidates.txt` joined `CURATED_FILES`) but always aggregated inside a
648-or-larger candidate batch, never cited as an isolated result for these
six tokens specifically, and predates this session's `whitespace_variants`
and literal-raw-key additions. Ran all three current oracles against
exactly these six tokens, isolated, for a clean citation:

```text
padded EXTENDED_CIPHER_VARIANTS (+ newline_variants): 108 attempts, 0 hits
padded EXTENDED_CIPHER_VARIANTS (no newline):          36 attempts, 0 hits
literal raw-key (zero-transformation, Phase 164):      48 attempts, 0 hits
nopad EC-scalar/address oracle (+ whitespace_variants): 144 keystrings,
                                                         0 errors, 0 classifications (not even vanity noise)
```

**Verdict.** No upstream link in the creator-ledger chain was found to be
wrongly pruned by this session's independent check -- the one place a real
instance of "leaf-testing past an upstream caution" occurred (`BUT`/`HYE`
as cipher-key seed material, post-Phase-34) had already been caught and
formally closed by the project itself (Phase 124), before this session.
The literal rail tokens are now also confirmed closed in isolation under
every current oracle, including this session's new additions. Per Phase
124's own stopping rule and this ledger's "Ranked next actions," the
correct next move is not another transform guess at any existing node --
it's new primary evidence (the physical book's pages 57-58, or any
not-yet-surfaced creator message), which this project has already
identified and has no code-side substitute for.

