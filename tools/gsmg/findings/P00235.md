## Phase 235 -- Architect-passage residual audit: screenplay/film provenance split, KEY/NOTE/SELF gap closed, tone commentary absent (2026-08-10)

`tools/gsmg/architect_passage_residual_audit.py` closes three small items
left open by the Phase 3.2.1 brainstorm thread (Phase 118, 232-234).

Two custom-passage words trace to the screenplay draft, not the film: `a
temporary dissemination of the code you carry` and `will ultimately result
in the extinction` both match the screenplay PDF exactly and are absent from
the film's actual spoken lines (confirmed directly against both primary
sources). One word-order choice traces the other way: the puzzle's `is now
to return` matches the film, not the screenplay's `is to now return`. This
shows the creator's wording blends both sources rather than following one
pinned document -- a real texture, not a password, and consistent with the
already-established two-sloppy-days construction (Phase 230).

`key`, `note`, `self`, and `keynote` are ordinary dictionary words already
swept as checkerboard keywords via Phase 2's `american-english` wordlist
pass (SALPH+COSMIC only), but none had ever been tested as a direct blob
password, and the literal contiguous `selfself` run had no coverage at all.
A bounded direct-password check (Phase 232/234's 18-keystring-form pattern)
for all five forms against all four tracked blobs: 90 keystrings x 4 blobs,
0 hits.

A two-corpus creator search for `architect`, `3.2.1`, and `matrix text`
returns zero hits in both corpora -- the creator never references this
passage by name anywhere. A wider tone-word search (`joke`, `kidding`,
`dramatic`, `theatric*`, `exaggerat*`, `parody`, `caricature`) surfaces 18
creator messages, all inspected and all unrelated (personal asides, IT
jokes, an unrelated bitconnect-parody idea). The brainstorm report's
"theatrical misdirection" reading therefore remains an inference from
general anti-bruteforce guidance (Phase 226/230), not a documented creator
statement about this specific passage.

Full report: [doc/GSMG_ARCHITECT_PASSAGE_RESIDUAL_AUDIT.md](../../../doc/GSMG_ARCHITECT_PASSAGE_RESIDUAL_AUDIT.md).
**Verdict:** provenance split confirmed but inconclusive; KEY/NOTE/SELF
family's direct-password gap closed and negative; theatrical-misdirection
reading correctly stays inference-only. No decoder or oracle expansion is
authorized.

