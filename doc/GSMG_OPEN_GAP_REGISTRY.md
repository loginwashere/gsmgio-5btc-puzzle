---
type: index
status: live
topics:
  - open-gap-registry
---

# GSMG Open-Gap Registry

Exactly what's blocking progress, one gap per row, each with a stable ID.
Distinct from [GSMG_STRICT_TRANSITION_WORKSHEET](GSMG_STRICT_TRANSITION_WORKSHEET.md)
(per-*candidate* rows, what operation might advance) and
[GSMG_FACT_LEDGER](GSMG_FACT_LEDGER.md) (per-*claim* rows, what's currently
accepted). This is per-*gap*: what specific missing evidence would unblock
something, independent of which candidate depends on it. A gap closing does
not mean a candidate is promoted — it means the worksheet's G1–G5 gates can
now be re-evaluated with that evidence in hand.

Most rows here restate a worksheet row's "Reopen trigger" column with a
stable ID and cross-link, rather than duplicating its reasoning — see the
linked row/audit for why the gap exists. Add a row only when it is not
already fully covered by an existing one.

## Registry

`Category` groups gaps by the kind of evidence that would close them.
`Priority` is P0 (blocks the live frontier directly) through P2
(narrative/speculative branch, no active work expected). `State` says
whether the gap is currently actionable: `open` (something can be done now
— a search, a re-derivation), `external` (blocked on a source outside our
control, e.g. an unphotographed book page), or `parked` (no near-term path
to evidence; revisit only if new material surfaces).

| Gap ID | Blocks | What's missing | Evidence type needed | Category | Priority | State | Next action | Closure condition | Canonical reference |
|---|---|---|---|---|---|---|---|---|---|
| G-MSL-001 | 31-char DBBI selection → `matrixsumlist` | No source binds the selection to matrix dimensions, traversal, value mapping, aggregation, or serialization (7/7 G3 fields unbound). The *Cosmic Duality* book's pages 57–58 were photographed and transcribed 2026-08-13 (Phase 259): page 57 is a full-page plate reprint of the already-known Franz Stuck "Sin" painting, page 58 continues the Eve/Tertullian/Council-of-Mâcon narrative and a Black Madonna sidebar — no matrix, dimension, or traversal content, and no operational numeric schema (the page's own dates/counts — 1893, 1656, 585, fifty-nine bishops, a majority of one — are ordinary historical narrative, not a selected/consumed value). All 7/7 G3 fields remain unbound after review. **Scope note (Phase 372, 2026-08-22, corrected same-day):** `matrixsumlist` is DBBI's own adjacent instruction (Phase 371), structurally separate from `thispassword`/`lastwordsbeforearchichoice` (FAED's own adjacent instructions) regardless of which of Phase 101's three unreconciled `thispassword` roles eventually wins — this gap is not shown to gate SALPH either way. This is a scope observation, not a relative-priority argument: this gap's own P0 rating reflects that it fully blocks consumption of the 31-character DBBI selection on its own terms, independent of any comparison to SALPH's gaps, and is left unchanged. **Phase 454 (2026-08-29):** the bounded recipient-side acquisition checked local Git recovery, repository/fork/ref/release surfaces, Internet Archive metadata, fixed discussion attachments, and eight exact search queries. Two independent captures strengthen already-known Stage-1 provenance, but no new primary artifact or matrix operation/consumer was recovered; gap unchanged. **Phase 458 (2026-08-30):** a frozen sweep of five Telegram-sensitive gaps (this one plus `G-ARCH-001`/`G-YIN-001`/`G-ESC-001`/`G-PRIME-001`, not the registry's other four parked gaps) over ids 67269-70186 (2,516 messages) found zero creator-authored messages, zero creator-authored media, and zero non-creator messages matching this gap's vocabulary with a creator reply. Of that span, 952 messages (ids 67269-68343) duplicate Phase 247's already-checked window; the genuinely new tail is 1,564 messages (ids 68344-70186, `ChatExport_2026-08-30`). This gap's `matrixsumlist`/dimension/traversal vocabulary was newly assembled for Phase 458, not previously frozen elsewhere. Gap unchanged. **Phase 468 (2026-09-01):** a closed-universe typed cross-reference found this is one of only two genuine `parameter_value` slots in the whole registry (a raw value could in principle fill it), then checked the selection's own length: `31` is prime, so zero nondegenerate rectangular factorizations `rows x cols = 31` exist, and the complete finite operation table over `{7,13,31,91}` (this project's own established DBBI-wide dimension facts) produces nine flagged equalities against the set, none nontrivial beyond the `7x13=91` construction identity (six are the trivially-guaranteed `a mod b = a` case for `a<b`; two more restate `7x13=91` as division). `31` itself is not a catalog row in this phase's established-outputs list, a recorded omission, not a finding about `31`. This rules out only an exact rectangular layout -- not padding, a `1x31`/`31x1` rail, ragged rows, sub-selection from a larger matrix, or independently-supplied dimensions. Gap unchanged | Primary source other than the *Cosmic Duality* book — a creator clue, recovered guide step, or code predating the output that uniquely fixes matrix dimensions, traversal, and operation | primary-source | P0 | parked | None planned; the book's pages 57–58 (the only remaining genuinely uninspected primary source) are now reviewed and negative. Revisit only if a new creator source, recovered guide, or pre-cutoff code artifact surfaces | A creator clue, recovered guide step, or code artifact is found that fixes the 7 G3 fields against the 31-character selection | [GSMG_STRICT_TRANSITION_WORKSHEET](GSMG_STRICT_TRANSITION_WORKSHEET.md#master-worksheet); [GSMG_TOPOLOGY_AUDIT](GSMG_TOPOLOGY_AUDIT.md); [GSMG_P454_RECIPIENT_ARTIFACT_ACQUISITION](GSMG_P454_RECIPIENT_ARTIFACT_ACQUISITION.md); [GSMG_P458_NEW_WINDOW_CREATOR_SWEEP](GSMG_P458_NEW_WINDOW_CREATOR_SWEEP.md) |
| G-FLOWER-001 | Rotated inverse-prime pair -> intended `yinyang` state / downstream consumer | Phase 461 fixes a bounded recognition chain from the authenticated 24 apertures through inverse primes `574061/311027` to a unique `THEFLOWER` hit inside its frozen orientation/composition family. What remains missing is independent selection of the 90-degree turn plus the frame/parity/affix composition, and any authenticated consumer. The exact old-password prefix is a strong checksum but was known before the family was formalized; direct blob consumers are negative and the same bounded grammar cannot derive `BLOSSOMS`. Phase 464 completed the preregistered creator-provenance audit: 5,901 creator messages, 586 licensed contexts, and all 88 creator-media records yielded zero exact `311027`/`04BEF3` references; all 14 rotation-plus-second-selector contexts and both media keyword hits were unrelated; no authenticated consumer was found. Phases 465-466 then tested the newly licensed internal reuse: the old credential is neither a privileged modular running key nor an exact checkerboard crib for DBBI/FAED. Phase 467's global closure retains `THEFLOWER` as one of three closest live edges, but its operation, direction/representation, and consumer remain unbound across all 27 maximum assignments | Creator clue, pre-discovery artifact, or authenticated consumer selecting at least two load-bearing operations or consuming the output | derivation | P1 | parked | Use `THEFLOWER` only as a closed-system router/checksum. Phases 465-466 reject the full old credential as a Phase-310/320 running key or exact DBBI/FAED crib. Phase 467 includes this branch but licenses no transform; target the four surviving selectors and do not hand-fit `BLOSSOMS` or extend the arithmetic family | A creator-authored or otherwise independent source selects the quarter-turn and another load-bearing operation, an authenticated artifact consumes the result, or a preregistered continuation predicts unseen material | [GSMG_ROSE_PRIME_CLOSED_CYCLE_AUDIT](GSMG_ROSE_PRIME_CLOSED_CYCLE_AUDIT.md); [GSMG_SECOND_PRIME_MATRIXSUMLIST_AUDIT](GSMG_SECOND_PRIME_MATRIXSUMLIST_AUDIT.md); [GSMG_BLOSSOMS_BOUNDARY_AUDIT](GSMG_BLOSSOMS_BOUNDARY_AUDIT.md); [GSMG_PHASE464_ROTATED_PRIME_PROVENANCE_AUDIT](GSMG_PHASE464_ROTATED_PRIME_PROVENANCE_AUDIT.md); [GSMG_PHASE465_PHASE1_RUNNING_KEY_AUDIT](GSMG_PHASE465_PHASE1_RUNNING_KEY_AUDIT.md); [GSMG_PHASE466_PHASE1_CREDENTIAL_CRIB_AUDIT](GSMG_PHASE466_PHASE1_CREDENTIAL_CRIB_AUDIT.md) |
| G-ARCH-001 | Architect words → BYE → CIAO BELLA O | No creator clue selects the beginnings/endings reading or the B↔H mirror operation itself. Phases 247–248 ran all five bounded brainstorm lanes: newer-export coverage, targeted selector vocabulary, Architect+selector replies/reactions, all 88 creator-authored media records (83 unique payloads), and direct precedent-transfer language — all negative. **Phase 372 (2026-08-22, corrected same-day):** the SALPH/COSMIC eligibility audit's original write-up asserted this gap's resolved output is `thispassword`'s sole required input and raised this row to P0 on that basis. On review, that overclaimed on two counts: Phase 101 (`salphaseion_operand_binding_audit.py`) explicitly retained three unreconciled roles for `lastwordsbeforearchichoice`/`thispassword` (password for FAED, FAED's answer merely labeled "password," or password for SALPH), none selected by any source — only the third role would even make this a SALPH dependency; and even under that role, "the Architect-choice output" is not established to be this gap's specific beginnings/endings/mirror-operation hypothesis rather than some other reading of "words before choice." Priority reverted to P1. This gap remains *one possible* dependency of `thispassword`'s (still underdetermined) SALPH-consuming role, not an established sole bottleneck — see [GSMG_TOPOLOGY_AUDIT](GSMG_TOPOLOGY_AUDIT.md)'s corrected frontier statement. The registry already carried `G-ESC-001`/`G-YIN-001` at P0, so "sole P0" was internally inconsistent regardless. **Phase 373 (2026-08-22, corrected same-day):** an attempt to discriminate the role question itself (not this gap's operand) by scoring dataflow topology on 7 dimensions was run and then retracted on review. The original write-up claimed `password_for_salph_blob` won uniquely and that `thispassword` "structurally targets the SALPH blob" — that claim is withdrawn. It rested on disputed feature assignments: `faed_answer_is_password` was scored as though it had to skip `lastwordsbeforearchichoice` to bind on raw FAED (its natural graph labels that instruction's own output directly, no skip needed), and `password_for_salph_blob` was scored against `hash_prefix` — a separate instruction with its own already-scoped SHA operand (Phase 121/372) — rather than the actual SALPH blob. A corrected modeling (`tools/gsmg/thispassword_role_topology_discrimination_audit.py`, fixed same-day) instead ranks `faed_answer_is_password` first, and the two modelings' disagreement is itself now an asserted, checked fact. Separately, the calibration boundaries (Phase 3, Phase 3.2) validate only "explicit consumption vs. checkpoint," not the postpositive/ambiguous attachment `thispassword` actually poses — no solved GSMG boundary calibrates that (`CALIBRATION_ANALOG_AVAILABLE = False`). Verdict: inconclusive/model-dependent; `operand_ranking_licensed = False`. This gap's specific operand remains one *candidate* reading of an *unresolved* role, not a live candidate for a resolved slot. Priority held at P1. **Phases 376-377 (2026-08-22, Phase 377 corrected same-day):** the role question itself (not this gap's operand) was checked for a direct role-selecting witness in primary evidence -- no scoring, no password generation. Froze the literal DOM stream (live-re-verified across all 5 Wayback captures), solved-stage grammar, and the full creator reply record (148 rows, mechanically extracted), then checked each of the three roles for a discriminating observable: none was found under the three declared tests (the literal stream carries directional/deictic vocabulary -- 'before', 'this', 'first', 'last' -- but no distinct explicit attachment marker; no solved boundary has the postpositive-label pattern `faed_answer_is_password` would need; the creator account never uses the word `thispassword` anywhere in the export, including a narrower check that none of the 148 creator reply edges has a parent message containing the term). No hard contradiction was detected under these declared checks (paraphrase-level creator evidence is explicitly unchecked). **Verdict (corrected -- the first write-up's "formally unidentifiable... unresolvable by any model" was an overclaim and is withdrawn):** no direct witness found under these three tests; the role remains underdetermined; further internal scoring is not licensed, so it stays parked. This is consistent with, not stronger than, Phase 373's inconclusive verdict. Priority unchanged at P1; this does not reopen the row -- it records that no witness turned up under the tests actually run, not that none could ever exist. **Phase 456 (2026-08-30):** Seed-1 transfer replayed the Architect edges/mirror rule exactly, but none of the three solved AES boundaries supplies a comparable local selector, edges/mirror instruction, and three-letter output role (0/3 applicable). This adds neither grammar support nor rejection; gap unchanged. **Phase 455 (2026-08-29):** the fourth, non-oracle typed semantic-checksum test checked all three `thispassword` roles on seven frozen axes. All three survive with zero contradictions; SALPH is typed as passphrase-consuming, but no authenticated fact binds `thispassword` to it rather than FAED or the preceding result. No role or operand is selected; this row remains only a possible dependency and its priority is unchanged. **Phase 452 (2026-08-29):** Phase 449's `G-ESC-001` pair-discrimination audit found the FAED `{h,e}` escape pair's exact mirror derivation "depends on the still-unselected G-ARCH-001 mirror operation" and fails `{h,e}` on a "load-bearing parked-gap dependency" gate specifically because of this row -- a previously-undocumented dependency, now recorded: this gap blocks `G-ESC-001`'s pair reconciliation in addition to its already-recorded `thispassword`/SALPH role. No new primary evidence about the mirror operation was found, so this is a cross-reference only; priority held at P1. Separately, Phase 451's BTCSEED/topology construction (DBBI/FAED branch) was checked and confirmed to have no bearing on this row, consistent with the Phase 372 scope-separation note (`G-MSL-001` row). **Phase 458 (2026-08-30):** the same five-gap sweep (see `G-MSL-001` row for span/overlap detail) found zero creator activity or licensed vocabulary hits for this gap either, reusing Phase 247's already-frozen mirror/architect/HYE vocabulary verbatim; gap unchanged. **Phase 467 (2026-09-01):** the global closure leaves the Architect relation as one of four varying selectors and one of three closest live edges. If independently selected, the proposed mirror would simultaneously force asymmetric `DBBI -> FAED` and the `HE` escape pair; this is dependency leverage, not new support. **Phase 470 (2026-09-02):** a four-selector actionability audit separates that conditional leverage from executability: zero of four selectors has a genuinely new current internal test. The previously queued solved/checksum-boundary mirror transfer duplicates Phase 456 (0/3 boundaries eligible), and same-endpoint FF67 reuse would not be blind. Architect remains highest conditional leverage, but no transform is licensed | Non-circular internal calibration or creator clue selecting this operation | derivation | P1 | parked | No currently executable internal selector test. Do not repeat Phase 456 against the same solved boundaries; revisit only when a genuinely new boundary has native edge inputs, a local mirror instruction, and a comparable output role, or when an independent pre-discovery artifact/consumer surfaces. Do not rescan the exhausted Telegram/media lanes or count `THEFLOWER` if its construction reuses the same frame vocabulary | An independent artifact or held-out boundary selects the beginnings/endings mirror and predicts an unseen output; alternatively, a creator source explicitly selects the operation | [GSMG_STRICT_TRANSITION_WORKSHEET](GSMG_STRICT_TRANSITION_WORKSHEET.md#master-worksheet); [GSMG_ARCHITECT_MIRROR_SELECTOR_AUDIT](GSMG_ARCHITECT_MIRROR_SELECTOR_AUDIT.md); [GSMG_TOPOLOGY_AUDIT](GSMG_TOPOLOGY_AUDIT.md); [GSMG_P452_G_ARCH_CROSS_GAP_DEPENDENCY_SYNTHESIS](GSMG_P452_G_ARCH_CROSS_GAP_DEPENDENCY_SYNTHESIS.md); [GSMG_P455_THISPASSWORD_TYPED_SEMANTIC_CHECKSUM](GSMG_P455_THISPASSWORD_TYPED_SEMANTIC_CHECKSUM.md); [GSMG_P456_SEED1_UNRESOLVED_RULE_TRANSFER](GSMG_P456_SEED1_UNRESOLVED_RULE_TRANSFER.md); [GSMG_P458_NEW_WINDOW_CREATOR_SWEEP](GSMG_P458_NEW_WINDOW_CREATOR_SWEEP.md); [GSMG_PHASE467_CLOSED_SYSTEM_CONSTRAINT_CLOSURE](GSMG_PHASE467_CLOSED_SYSTEM_CONSTRAINT_CLOSURE.md); [GSMG_PHASE470_FRONTIER_ACTIONABILITY_AUDIT](GSMG_PHASE470_FRONTIER_ACTIONABILITY_AUDIT.md) |
| G-ESC-001 | FAED decoder selection | FAED's independently-best `{g,i}` escape pair and the Architect-mirror-predicted `{h,e}` pair remain unreconciled; neither pair's mirror9 image validly segments its own origin stream. Phases 243/244/249 closed the entire page-boundary branch: textarea attributes, CSS, JS, comments, whitespace, and byte-verified content across 16 successful archive capture events (5 Wayback + 11 urlscan, 2023-05-31 to 2026-04-05) supply no selector; DOM ancestry/sibling selectors are inapplicable by construction since DBBI/FAED share one text node. Phases 271-321 separately ran ~50 direct cryptanalytic/structural models over DBBI/FAED under both candidate segmentations without a page-boundary dependency — all negative or null-like. **Phase 449:** both pairs validly tokenize complete FAED and use all 25 code types. `{g,i}` decisively outranks `{h,e}` inside the ordinary-English checkerboard model (code-IC rank 1 vs. 16; 73.75% vs. 2.19% of FAED's non-uniform chi-square), so it is the working prior, but those are model-conditional statistics rather than an authenticated selector. `{h,e}`'s exact mirror derivation depends on parked G-ARCH-001, the creator `YING -> IG` observation fails its authorship gate, and the escape-independent Bifid `BTCSEED` checkpoint cannot discriminate. Neither pair is contradicted at pair level; the gap remains unreconciled. **Phase 454 (2026-08-29):** the bounded recipient-side acquisition found two known-byte Stage-1 capture confirmations but no new SalPhaseIon page variant, creator source, selector, or pair-independent validator; gap unchanged. **Phase 458 (2026-08-30):** the same five-gap sweep (see `G-MSL-001` row for span/overlap detail) found zero creator activity or licensed vocabulary hits for this gap's escape/mirror9/pair terms, using a vocabulary newly assembled for Phase 458 rather than a previously frozen protocol; gap unchanged. **Phase 468 (2026-09-01):** a calibration check of the `FF67`/`(255,103)` matrix-product endpoint's ASCII-escape coincidence with this gap's candidate pairs, replaying Phase 453's own null-generation functions under two frozen hypotheses (`{g,i}`; the union `{e,g,h,i}`) -- both cross Holm-adjusted p < 0.005 under both a 100,000-sample and the full 151,200-permutation null population (Holm p=0.000560/0.000648). This is a post-observation conditional rarity calibration, not a confirmatory discovery result: the frozen protocol itself records that this numeric conjunction "turned up during that inventory" before the two hypotheses were frozen, so the Holm correction covers the frozen 2-hypothesis x 2-population family but not the prior selection of this specific conjunction out of the broader known-parts inventory. Records the observed calibration result; absent a preregistered surviving selection criterion, it does not select an escape pair or operator. `{h,e}` was never a tested hypothesis, so no comparison between the two pairs is licensed. Gap unchanged **Phase 477A (2026-09-04):** Model A token columnar transposition (plaintext transposed before the checkerboard) is a bounded negative for `{g,i}` at widths 2-40 transpose / 2-11 untranspose (`p = 15/201`) and unpromoted for `{h,e}` at width 7 (`p = 3/201`); the calibration is score-reach on 0.62-0.658 concentration fixtures, so the missing experiment is a separately locked exact-histogram sensitivity test on planted sequences built from FAED's exact 25-token multiset, plus untranspose widths 12-40 and the raw-digit VIC order (477B). | A genuinely external source — a creator clue, a primary artifact outside this page, or a capture with a main-document hash outside the 5 authenticated Wayback variants — that independently selects one pair, or explains why the two pairs need not reconcile | derivation | P0 | parked | None planned; the page-boundary branch and both known archive surfaces are exhausted. Revisit only if a new creator source, primary artifact, or genuinely new page variant surfaces | A genuinely external source agrees with one pair, explains why reconciliation is not required, or fixes a decoder with a pair-independent validator | [GSMG_P449_G_ESC_PAIR_DISCRIMINATION](GSMG_P449_G_ESC_PAIR_DISCRIMINATION.md); [GSMG_ARCHITECT_CHOICE_BOUNDARY_AUDIT](GSMG_ARCHITECT_CHOICE_BOUNDARY_AUDIT.md); [GSMG_DBBI_FAED_BOUNDARY_SELECTOR_AUDIT](GSMG_DBBI_FAED_BOUNDARY_SELECTOR_AUDIT.md); [GSMG_SALPHASEION_URLSCAN_HISTORY_AUDIT](GSMG_SALPHASEION_URLSCAN_HISTORY_AUDIT.md); [GSMG_P454_RECIPIENT_ARTIFACT_ACQUISITION](GSMG_P454_RECIPIENT_ARTIFACT_ACQUISITION.md); [GSMG_P458_NEW_WINDOW_CREATOR_SWEEP](GSMG_P458_NEW_WINDOW_CREATOR_SWEEP.md) |
| G-YIN-001 | DBBI/FAED → creator's `yinyang` state | No operator/relationship is selected between the two raw streams at all — a live semantic boundary, not yet executable. Phase 262 (2026-08-13): `(page - A1Z26(letter)) mod 26` over Chapter 2's first three yin-yang-decorated drop caps (pixel-confirmed W/48, O/50, O/55) spells `YIN` exactly — genuinely interesting given this gap's subject, but no `YANG` counterpart found, nothing selects "first three of Chapter 2" as the input scope, and every other tested operation was negative. Bounded lead, not a mechanism. Phases 271-321 (2026-08-14 to 2026-08-19) additionally ran ~50 direct cross-stream coupling models against DBBI/FAED — transition matrices (275), mirror9 substitution (293), positional co-occurrence (297), GF(9)/base-27/base-81/FSM/sequence-alignment presentations, Bacon (309), Nihilist additive-key (310), a Cosmic-Duality-text running key (311), Bellaso 1553 (312), ADFGVX transposition (321), and Gronsfeld/progressive shifts (320) — all negative or null-like, none supplying an operator. **Sharpened by Phase 369 (2026-08-22):** this row's framing ("no operator is selected") is one level too narrow — the topology audit found all ~50 of those phases assume DBBI/FAED must combine (the row's own implicit premise) and test *which* operator, but none tests *whether* an operator is needed at all. **Phase 371 (2026-08-22) ran that test directly** (per-stream, against the literal page structure, not by forcing DBBI/FAED through Phase 341's grammar): DBBI has exactly one adjacent instruction token (`matrixsumlist`, present but unexecutable per `G-MSL-001`); FAED has two (`lastwordsbeforearchichoice`, `thispassword`, pointing at `G-ARCH-001`'s subject, not demonstrated to consume FAED itself); neither instruction embeds its stream's raw content; each stream's own independently-best escape pair differs (`{b,e}` vs. `{g,i}`); no page evidence requires either stream as input to the other's consumer. Inconclusive by design (a checkpoint/no-consumer read was an explicitly permitted outcome), but it is real, executed evidence against symmetric-combine (T3) framing, not merely an untested alternative anymore. **Phase 451 (2026-08-29):** reviewed the community `BTCSEED` branch (Phases 386-408 — a Bifid square keyed from `DBBI[:13]` decrypting `FAED`) against this row. It is topologically T4-shaped (`DBBI` instructs `FAED`, asymmetric), not T3-shaped, a cross-mapping not previously stated; it is real and mechanically reproducible but stopped, not promoted (checkpoint survives only under the single period used to discover it, zero consumer hits across 12 frozen families); it does not contradict Phase 371 (different evidence classes: page-licensing vs. an unlicensed executed construction) or Phase 412/413 (which reject only the symmetric shared-distribution null, not asymmetric constructions). Per Phase 386's own disposition, this "does not reopen or narrow any existing gap" — disposition unchanged. **Phase 454 (2026-08-29):** recipient-side acquisition recovered no creator evidence defining a DBBI/FAED interaction and no genuinely new page artifact; gap unchanged. **Phase 458 (2026-08-30):** the same five-gap sweep (see `G-MSL-001` row for span/overlap detail) found zero creator activity or licensed vocabulary hits for this gap's yin/yang/DBBI/FAED terms, using a vocabulary newly assembled for Phase 458 rather than a previously frozen protocol; gap unchanged | Creator evidence defining how the two streams interact, or (narrower) a `YANG` counterpart / explicit selector for the Chapter-2 drop-cap window | creator-clue | P0 | parked | None planned; the Telegram corpora (Phase 52, 124, 215), every decoded-slot page-syntax house rule (Phase 238), the extended cross-stream coupling sweep (Phases 271-321), the independent-consumer audit (Phase 371), and the BTCSEED topology synthesis (Phase 451) are all exhausted for this row. Revisit only if a new export or genuinely new page artifact surfaces | A creator source, or a structurally forced single reading not already ruled out by Phase 238, selects an operator | [GSMG_STRICT_TRANSITION_WORKSHEET](GSMG_STRICT_TRANSITION_WORKSHEET.md#master-worksheet); [GSMG_TOPOLOGY_AUDIT](GSMG_TOPOLOGY_AUDIT.md); [GSMG_FRONTIER_ASSUMPTION_LEDGER](GSMG_FRONTIER_ASSUMPTION_LEDGER.md); [GSMG_P451_G_YIN_BTCSEED_TOPOLOGY_SYNTHESIS](GSMG_P451_G_YIN_BTCSEED_TOPOLOGY_SYNTHESIS.md); [GSMG_P454_RECIPIENT_ARTIFACT_ACQUISITION](GSMG_P454_RECIPIENT_ARTIFACT_ACQUISITION.md) |
| G-PRIME-001 | Prime lists → `401/400/73` | No instruction or key consumes the three sums; no downstream target. Phase 260/261: title initials Roman `CD=400` exactly match yellow, but decoration is book-wide house style, not a title-specific selector. Phase 263: strict Roman projections `DBBI->DI` and `FAED->D`, each prefixed by title `C`, give canonical `CDI/CD=401/400` in exact blue/yellow order. This is the unique ordered hit in 14 bounded rail configurations, but a disclosed 1,092-configuration token control has a second `yinyang/FEFE` hit; no clue selects Roman filtering or title `C`, and FEFE's actual `73` is still unconsumed. Corroboration only, not a G3 operation/consumer. **Phase 450 (2026-08-29):** applying the exact winning rule (title `C` prefix, no new choice) to FEFE itself gives `C + roman(FEFE) = C + "" = 100`, not `73` -- a checked negative, not merely unexplained. A pre-registered sweep of the complete 55,963-message Telegram export for co-occurring `CDI`/`CD` tokens, the phrase `roman numeral(s)`/`title initial`, and the standalone triple `401`/`400`/`73` found zero, two, and two hits respectively; every hit is non-creator and content-unrelated (a generic pasted prime table, an unrelated FAED letter-position count, and two ROT-cipher/unrelated-clue remarks). No consumer or selector found; gap unchanged. **Phase 456 (2026-08-30):** Seed-1 transfer replayed the Roman/title-C rule exactly, but 0/3 solved AES boundaries locally selects Roman projection, a title-C prefix, and numeral-to-password serialization. No comparable prediction exists; gap unchanged. **Phase 453 (2026-08-29):** under the frozen symbolic false-discovery harness, the Roman target is common after four-case Holm correction under both empirical-character and uniform-A..Z token nulls (adjusted p=0.075419 and 0.069799); no gap closure. **Phase 458 (2026-08-30):** the same five-gap sweep (see `G-MSL-001` row for span/overlap detail) found zero creator activity or licensed vocabulary hits for the standalone triple, Roman forms, or selector phrases, reusing Phase 450's already-frozen vocabulary verbatim; gap unchanged | Explicit clue consuming all three sums, or selecting Roman projection/title `C` and explaining FEFE/73 | creator-clue | P2 | parked | None planned; revisit only if a new consumer candidate or selector is proposed | A clue or downstream artifact consumes all three sums, or independently selects the Roman construction and resolves FEFE/73 | [GSMG_STRICT_TRANSITION_WORKSHEET](GSMG_STRICT_TRANSITION_WORKSHEET.md#master-worksheet); [GSMG_P450_G_PRIME_CONSUMER_SELECTOR_SWEEP](GSMG_P450_G_PRIME_CONSUMER_SELECTOR_SWEEP.md); [GSMG_P453_FALSE_DISCOVERY_CALIBRATION](GSMG_P453_FALSE_DISCOVERY_CALIBRATION.md); [GSMG_P456_SEED1_UNRESOLVED_RULE_TRANSFER](GSMG_P456_SEED1_UNRESOLVED_RULE_TRANSFER.md); [GSMG_P458_NEW_WINDOW_CREATOR_SWEEP](GSMG_P458_NEW_WINDOW_CREATOR_SWEEP.md) |
| G-MATPROD-001 | Matrix product → `(255,103)` / `FF67` | Multiplication as the operation, and a byte consumer, are both unselected. **Phase 456 (2026-08-30):** Seed-1 transfer replayed the matrix product/FF67 rule exactly, but 0/3 solved AES boundaries has the native six-digit 2x3/sum-list type, multiplication instruction, and byte consumer. No comparable prediction exists; gap unchanged. **Phase 453:** the FF-plus-ASCII-letter endpoint is unusual under both frozen distinct-decimal-matrix nulls (Holm-adjusted p=0.013960 and 0.013042), but remains unselected and supplies no consumer | Clue selecting multiplication and a byte consumer | derivation | P2 | parked | None planned; revisit only if a new consumer candidate is proposed | A clue selects multiplication and names a byte consumer | [GSMG_STRICT_TRANSITION_WORKSHEET](GSMG_STRICT_TRANSITION_WORKSHEET.md#master-worksheet); [GSMG_P453_FALSE_DISCOVERY_CALIBRATION](GSMG_P453_FALSE_DISCOVERY_CALIBRATION.md); [GSMG_P456_SEED1_UNRESOLVED_RULE_TRANSFER](GSMG_P456_SEED1_UNRESOLVED_RULE_TRANSFER.md) |
| G-KIT-001 | Second matrix list difference → reversed `KIT` | Subtraction, A1Z26, and reversal are three unselected steps. **Phase 453:** the broader BIP39-word endpoint is sensitive to null design (Holm-adjusted p=0.048270 versus 0.36), so KIT gains no calibrated promotion | Clue explicitly asking for difference/reversal or a young rabbit | creator-clue | P2 | parked | None planned; revisit only if a new consumer candidate is proposed | A clue explicitly requests difference/reversal or names the rabbit reading | [GSMG_STRICT_TRANSITION_WORKSHEET](GSMG_STRICT_TRANSITION_WORKSHEET.md#master-worksheet); [GSMG_P453_FALSE_DISCOVERY_CALIBRATION](GSMG_P453_FALSE_DISCOVERY_CALIBRATION.md) |
| G-GGN-001 | FEFE tuple `{1,4,21}` → `ggn` → secp256k1 | Indexing convention, `g→G`, scalar `k`, negation, and curve are all unselected. **Phase 453:** the repeated-pair/unique-third structure is common after four-case Holm correction under both frozen nulls (adjusted p=0.075419 and 0.054815); the curve narrative remains explicitly uncalibrated. **Phase 468 (2026-09-01):** this is the other of only two genuine `parameter_value` slots in the whole registry (scalar `k`); checked whether any of five newly-established checkpoint values (`574061`, `311027`, `33414671`, `THEFLOWER`, the 103-digit SALPH stream) work as secp256k1 key material under three separately-reported interpretations (direct-bytes-as-scalar, SHA-256-as-scalar, existing BIP32 seed-form/path enumeration) against the frozen 10-address target set -- zero hits across all candidates and interpretations (34 BIP32 address checks per candidate). Closes only this specific delta for these five candidates; does not weaken their broader structural significance. Gap unchanged | Independent clue supplying `k` and selecting group order/negation | creator-clue | P2 | parked | None planned; revisit only if a new consumer candidate is proposed | An independent clue supplies `k` and selects group order/negation | [GSMG_STRICT_TRANSITION_WORKSHEET](GSMG_STRICT_TRANSITION_WORKSHEET.md#master-worksheet); [GSMG_P453_FALSE_DISCOVERY_CALIBRATION](GSMG_P453_FALSE_DISCOVERY_CALIBRATION.md) |
| G-X2SH-001 | `X2SH4Y0QB15` → Decentraland four-point route | No creator source selects the `(2,0),(32,82),(-42,-16),(4,15)` reading over any other slicing of the same values. Phase 264 (2026-08-13): all four points independently verified against the live Decentraland Catalyst API as real scenes (spawn region, art gallery, one parcel from the authenticated GSMG audio piece, and — after reversal — beside the HTC/ZION scene), and message `1837` ("Only -41,-17 matters") is genuinely creator-authored, but its context is ordinary interactive-mode chat, not a route-selecting clue. Chronology blocks intent: the `(32,82)` gallery scene was deployed 2020-04-19, after the earliest observed repost of the clue (2020-03-24), and the full four-waypoint reading doesn't appear in the solver corpus until 2021 — a year later. The matching rule is also inconsistent (one exact point, one broad region, two adjacencies, one reversal), and the four scenes belong to three unrelated wallets | A creator statement selecting this four-point reading, or evidence resolving the chronology conflict (an earlier attestation of the `(32,82)` scene, or a later origin date for the clue) | creator-clue | P2 | parked | None planned; this is a closed, already-solved phase's secondary reading, not on the live frontier. Revisit only if a new chat export or creator statement surfaces | A creator source selects this reading, or the chronology conflict is resolved in its favor | [GSMG_PHASE2_DECENTRALAND_COORDINATE_AUDIT](GSMG_PHASE2_DECENTRALAND_COORDINATE_AUDIT.md) |

## Adding a gap

A gap earns a row only if it is genuinely open (not one of the worksheet's
closed rows — those need primary evidence fixing a missing parameter or a
demonstrably incorrect prior audit to reopen at all, per the worksheet's own
rules, and belong in [MOC - Negative Results](MOC%20-%20Negative%20Results.md)
instead) and is not already covered by an existing row. Close a row (set
`status: closed`, do not delete it) when its "Evidence type needed" is
actually supplied — closing a gap is not the same as promoting the
candidate it blocks, which still needs its own G1–G5 pass.
