---
type: index
status: live
topics:
  - creator-provenance
  - telegram
aliases:
  - Creator-Authored Clue Ledger
---

# GSMG Creator-Authored Clue Ledger

For the complete Telegram message-ID index—including direct confirmations,
praise/progress reactions, caveats, reply parents, and the July 2026 creator
messages—see [doc/GSMG_CREATOR_CLUE_AND_CONFIRMATION_INDEX.md](GSMG_CREATOR_CLUE_AND_CONFIRMATION_INDEX.md). This ledger
remains the analytical dependency-chain view.

## Scope and source discipline

This ledger uses only messages authored by `Jrk Bgrt` in the creator-only
Telegram export:

`/home/loginwashere/projects/gsmgio-5btc-puzzle/_work/creator_jrk.txt`

The export contains **411 creator messages** (411 `=== ... Jrk Bgrt` headers),
not 444. Its 1,283 count is the number of text lines, not messages. The full
chat transcript was used only to restore the conversation immediately around a
creator message; community claims are not treated as clues or confirmations.

Phase 62 independently maps all 411 exact creator headers to Telegram's stable
creator ID `user9815232` in the complete JSON export. None is deleted or
misattributed. Generic old-transcript sender labels are less reliable around
forwarded/quoted messages, so community provenance should use JSON message and
user IDs when available.

The goal here is not to generate more password guesses. It is to identify the
specific operation or value that each creator statement supplies, and the
specific variable that remains unresolved.

> **2026-07-26 supersession notice:** Phases 48-50 corrected the later
> Phase-47 material summarized near the bottom of this ledger. FEFE is a
> separate inserted event before color object 21, not a replacement/removal
> of that object. The exact 25-event prime walk now recovers Denis/Flo's
> 31-position mask and crosses into the following binary `matrixsumlist`
> segment. See [doc/GSMG_PHASE_BOUNDARY_REAUDIT.md](GSMG_PHASE_BOUNDARY_REAUDIT.md); the old adjacent-swap and
> two-channel-removal conclusions are historical, not current.

## Creator-only dependency ledger

| Date | Creator-authored clue | What it actually establishes | Unresolved operation or value | Status |
|---|---|---|---|---|
| 2020-01-14 | “Yellow has a number and so does Blue. Go back to the first puzzle piece…” | The first puzzle artifact's 24 colored LSB cells form two complementary numbers in the already-validated spiral order: blue=1/yellow=0 gives rose RGB `F73D92`; yellow=1/blue=0 gives prime `574061` (`08C26D`). | How the recovered prime, and possibly its rose-colored complement, feeds `matrixsumlist`. | **Reconstructed 2026-07-25** |
| 2020-05-20/21 | “answer is there”; “First or zero” | Confirms the other-door answer is in the first puzzle piece. For the FEFE cell, zero-based spiral numbering gives prime `163` while one-based gives composite `164`, objectively resolving this local ambiguity. | Whether “first” has an additional meaning elsewhere in the chain. | **Zero-based FEFE index resolved** |
| 2020-08-02 | “Really nobody managed to find the extra door…” | Confirms that “another door” is literal puzzle progression, not merely poetic language. | Exact extraction from the first artifact. | Confirmation only |
| 2021-03-01 | “You are at the prime part already???” followed by “That might have been a hint.” | Prime numbers are an intended operation or selector in the relevant path. | Which primes, whether primes select positions or values, and the object to which they apply. | **Primary unresolved operation** |
| 2021-03-14 | “Breaking salphation, should be giving the feeling of the phase's name.” | A correct result should have a semantically recognizable effect associated with the phase name. | The intended meaning of `SalPhaseIon` and whether it validates plaintext, a transition, or a destination. No concrete transform is specified. | Validation cue, not an algorithm |
| 2021-04-01 | `Hint: "another door might be found on {1 },{4} ,{21}"` | The values camouflage themselves as the post's `01.04.2021` April-Fools date, but they also identify the first grid's anomaly exactly: **one** `FEFE` cell, at the **4th bit** of the **21st character**. In the validated spiral it is zero-based position **163**, a prime, and its decoded bit is already zero. | What operation consumes prime position `163` or the marked character `n`; literal route/hash variants are negative. | **Exact FEFE locator reconstructed** |
| 2021-04-01 | `R=18 / A=1 / B=2`; “Could also be 21 or 1812 bit” | `18-1-2` is A1Z26 `R-A-B`; appending the literal word `bit` yields `RABBIT`, confirming that `{1,4,21}` should be applied to the rabbit bit grid. This follows discussion of the one exceptional pixel and the 15 blue/9 yellow/1 special-cell counts. | The wordplay does not by itself specify what follows the located zero at prime position 163. | **First-piece pointer confirmed** |
| 2021-12-26 | The other-door hint “is still a thing”; prime numbers are “required to proceed”; some characters must be “zeroed out.” | The FEFE locator supplies one concrete zero-valued marker at zero-based prime position `163`, inside character `n`. It validates the located object but does not exhaust the creator's plural “characters.” | Which larger character set is selected by primes and what “zeroed out” means operationally. | **One marker resolved; plural operation unresolved** |
| 2021-12-31; 2023-05-02 | “The only date I give away is the expiry date of neo's passport”; later emphasizes the expiration date again. | Most plausibly reinforces date recognition: the prop reads `11 SEP 01`, while `{1,4,21}` exactly matches the door-hint post's own `01 APR 21` date. Exact inscription forms are direct-oracle negative (Phase 117). | Whether date syntax does anything beyond validating the dual-use `{1,4,21}` locator; no creator message supplies a further date operation. | **Likely format clue; no downstream operation** |
| 2022-12-11; 2023-01-08 | A missing post is “very specific”; `@barrystyle` “provided a very specific hint already.” | The raw transcript identifies `semaj` as `@barrystyle`: he says he Googled a phrase assumed to belong to a hint (`chat_transcript.txt:20027`); the creator names barrystyle (`:20070`); later a participant asks what he searched “to get to the book” (`:71524`) and `semaj` answers `cosmic duality: mysteries of the unknown` (`:71527`). The missing post is therefore the book discovery, not an unknown prime diagram. | Exact media bytes remain absent, but semantic content is recovered. The pages 57-58 gap is independently established by the screenshot inventory and page-56 cross-reference, not inferred from chat. | **Evidence gap substantially closed** |
| 2023-01-09 | “At least prime number is very important to get any further.” | Reconfirms prime use after the book clue. | Singular “prime number” supports a derived prime but does not distinguish the 24-color complement `574061` from other constructions without further validation. | Strong confirmation |
| 2023-01-12; 2023-01-25 | “Focussing on the theory of everything is also still a valid path…”; replies `42` when asked how much math is in the puzzle. | At most supports connecting puzzle components; `42` is creator-authored but occurs as a humorous answer. | No bounded operation follows from these statements alone. | Low-priority/meta |
| 2023-02-23 | Reversed full-bitstream message decodes to `yellowblueprimes matrixsumlist lastwordsbeforearchichoice yinyang ...` | Gives an ordered dependency chain. The cross-source-stable forward-one indexing reconstructs `574061 -> [23,16,7] -> BOTH/ULTIMATELY/THE -> BUT/HYE`; in the `a-i` alphabet, `B <-> H` are mirror opposites while `E` is fixed. Phase 232 additionally shows that mirroring `a-i` symbols in the full endings rail while preserving `Y` gives `HYE -> BYE`, the only dictionary output among 48 stable `BUT` controls. | Which operation or lock follows `lastwordsbeforearchichoice`. `BYE` is a strong recognition candidate, not a creator-confirmed yin-yang state: preserving `Y` and transforming only the endings rail remain unauthenticated. Its 18-form direct/SHA blob family is negative. The former `H`/initials-of-“your eyes”/`BUT` rebus was **removed in Phase 217 as circular**, and the `{h,e}` FAED hypothesis remains strongly deprioritized. | **Ordered clue; BYE recognition conditional** |
| 2023-08-06 | “Once you hit a ‘ying yang’, you'll be able to solve it the same day”; asked “hit or hint?”, replies “Both?”; “You have all the info.” | Yin-yang is a recognizable reached state or next-stage artifact, not evidence that the literal words are a password. | What artifact or realization counts as reaching it. | **Recognition cue** |
| 2024-11-29 | “1 microstep further” would likely solve the puzzle the same day. | The remaining blocker is probably one local interpretation/transition, not another broad cipher family. | Which unresolved edge in the creator's chain is the microstep. | Search-priority guidance |
| 2025-04-28 | “when yingyang is reached, 2 hours max”; “It’s the next phase.” | Explicitly classifies yin-yang as the next phase and again predicts rapid completion after reaching it. | Which lock follows the reconstructed rails. | Strong confirmation |
| 2026-03-03 | Reaching the next phase means the prize is taken “in no-time.” | Confirms that downstream work after the missing transition is short. | None; timing/validation only. | Confirmation only |
| 2026-03-03 | Denis asks whether “in front of your eyes” recommends *Looking Forward*. Creator replies, “Maybe, Cartman's quote about chatroulette fits too,” then `🤐`. | Keeps the book possible but explicitly hedged; this is not confirmation. | Whether the book was intended or merely one fitting association. | **Community lead, creator-hedged** |
| 2026-03-04 | Minutes later, creator points at gnomad; gnomad repeats only “its in front of your eyes but you're not seeing it”; creator replies `Bingo`. | Confirms the phrase itself matters. Gnomad does not mention *Looking Forward*. Phase 366 additionally fixes the chronology: `Bingo` precedes Denis's first abstract 30–31-character claim by 13 seconds, the exact text by 601 seconds, and the narrated chain by 1,705 seconds, so it cannot authenticate those later claims. | What visible artifact or already-present text the phrase identifies. | **Phrase confirmed; eyes-to-31 binding rejected** |

## What the clues jointly say

The creator-only evidence supports this dependency graph:

```text
first/zero puzzle piece
    -> blue=1/yellow=0: F73D92 (rose RGB)
    -> yellow=1/blue=0: 08C26D = 574061 (prime)
    -> 18-1-2 + BIT = RABBIT / first-piece bit grid
    -> {1,4,21}: one FEFE cell, bit 4, character 21
    -> zero-based spiral position 163 (prime), bit value 0
    -> consume the marked prime/zero object
    -> matrix sum/list
    -> last words before "archi" choice
    -> beginning/end rails BUT / HYE
    -> B <-> H while E is fixed: bounded mirror9 state
    -> conditional full-rail reading HYE -> BYE
    -> UNKNOWN operation after the recognition checkpoint
    -> short final solve
```

The precise ordering of the middle steps is not fully fixed by prose alone.
The 2023 binary message fixes the macro order
`yellowblueprimes -> matrixsumlist -> lastwordsbeforearchichoice -> yinyang`;
the 2021 message places character-zeroing “along the way” in the door/prime
path but does not identify its exact position.

## Coverage versus actual gaps

Previous probes tested many literal forms of `1,4,21`, prime indices, the Neo
passport date, generic color masks, complement transforms, and direct AES
passphrases. Those tests missed the tuple's grounded object: it is a
hierarchical descriptor of the one FEFE cell in the rabbit bit grid, not
three positions in an arbitrary sequence.

In particular:

- `door_prime_passport_probe.py` treated `1,4,21` as direct passphrases and
  tried generic prime-position zeroing rules. It did not model the exact
  one-cell/bit-4/character-21 hierarchy or the resulting prime position 163.
- Generic yin-yang complements (`mirror9`, dual-ternary, dual-quinary, and
  related transforms) tested plausible cipher families. The creator only says
  yin-yang is a reached phase, so those negatives should not be read as
  exhausting the clue.
- The adjacent-difference sweep addresses a cipher hypothesis not specified by
  any creator clue. Its clean screen-negative result should remain closed
  unless new primary evidence points back to it.

## Ranked next actions

1. **Keep the Fe-plated result as a local checksum.** Deleting the addressed
   `n` from `theseedisplanted` gives `theseedisplated`; the anomalous RGB value
   supplies `FE FE FE`, where `Fe` is iron. The physical/on-chain referent
   audit found no authenticated seed-plate object, and the complete bounded
   family that coats literal `SEED` with the observed Fe units is negative
   under CBC, Key Wrap, and direct-private-key checks. Do not expand into
   products, alloys, element tables, or transaction fields without a new
   creator clue.
2. **Treat the barrystyle artifact as the already-investigated book lead.**
   Its exact media bytes remain missing, but the 2022/2023/2024 conversation
   recovers its identity. Do not use it as justification for an unspecified
   transform. The only physical-book evidence gap is pages 57-58.
3. **The `{h,e}` checkerboard reading of `faed` is now closed, not just
   keyword-negative.** Exact-seed, curated chain-addition, curated autokey,
   the first 54,250 large-dictionary alphabet candidates, AND a ciphertext-
   only monoalphabetic hill-climb against a proper same-budget shuffled-null
   control (FINDINGS.md Phase 43, corrected after external review) are all
   negative -- the real score falls inside the null range, not above it.
   Reinforced by a structural finding that doesn't depend on any search:
   `faed`'s escape-character-density under `(h,e)` (22.28%) is a poor match
   to this puzzle's own validated ~47%/~53% reference profile, and remains
   far outside what any corpus window of the matched length can reach --
   true of most but not literally all of the 29 pairs that segment cleanly
   (`{g,i}` is a rare-but-real exception, reachable at 7/2029 chat-corpus
   windows of its own matched length; already separately closed via
   chain-addition/autokey, Phase 16/38).
   The unrun autokey continuation `[54,250,338,905)` remains coverage
   completion of a keyword search, not a new model, and is lower priority
   than re-examining whether `(h,e)` is the right pair at all.
4. **Keep the FEN cross-check secondary.** Appending the removed `N` to each
   `FE` color channel yields `FEN FEN FEN`, matching the authenticated earlier
   chess notation. Broadcasting one character across three channels is not
   creator-specified, so do not use it to license a transform or sweep.
5. **Do not reopen zeroing mechanics.** Setting the marked bit to zero is a
   no-op; literal `0` and NUL replacements are non-language; deletion alone
   gives `theseedisplated`. Direct route/hash variants remain closed.
6. **Defer unrelated broad compute.** The unrun autokey continuation and circular/longer-lag
   adjacent transforms increase coverage but do not target the creator-identified
   missing operation. Run them unattended only after the evidence-recovery work.

## Bottom line

The creator chain is reconstructed through
`F73D92/574061 -> [23,16,7] -> BUT/HYE`. Partial mirror9 over the full endings
rail yields `BYE`, the sole dictionary output in its controlled `BUT` family,
but the pass-through/polarity convention remains unauthenticated and its
bounded blob family is negative. The resulting `B<->H` with fixed `E` therefore
remains an unconfirmed recognition state; the former
rebus built from `H` plus the initials of “your eyes” was circular and is
excluded. *Looking Forward*
is also unconfirmed: it is a thematically fitting community suggestion
that received a hedged creator response. Only the “in front of your eyes”
phrase itself has the later `Bingo` confirmation.
The matrix and FEFE branches converge at the unique cell
`spiral0=163, coordinate0=(7,4)`. The artifact's right-side rabbit tail
supports the row-tail selector, but Phase 48 supplies FEFE's stronger
mechanical role: it is inserted as its own prime-walk event before color
object 21. Deleting the addressed `n` to obtain **“the seed is Fe-plated”**
remains a semantic rebus with negative referent checks, not the leading
transition operation.

The exact walk selects 31 characters from the aligned Phase 3.2 plaintext and
continues for two ordinary-rate checkpoint bits into `matrixsumlist`. Phases
51-52 find neither a unique bounded consumer nor a creator-supported
post-selection operation in the archived text. Denis's four manually crafted
phrases are exact anagrams, but he explicitly reported that anagramming did not
produce the key. Literal `yang` may therefore be a terminal recognition
checkpoint rather than input to another transform; it is not a password or a
confirmed `"ying yang"` transition.

The local lock audit is negative. The creator-validated `barrystyle` artifact
is identified as the Cosmic Duality book lead, whose available text has
already been investigated. A fresh Telegram export recovers Denis's missing
guide through its reply metadata. It specifies a 14x14 placement and row-sum
operation yielding `IZLKESEEDQPPEN`, but uses the old two-mismatch event
model. Phase 53's pre-registered three-policy corrected-FEFE family is negative
(`p=0.0802`, independently replicated at `p=0.0818`); the historical output
itself is only weakly exceptional under the same gate (`p≈0.0384`), and a
direct Telegram reply confirms that ambiguous `b`/`be` choices were made to
match prime positions. No
creator-authored downstream instruction or AES escalation follows. The book's
physical pages 57-58 remain a separate evidence gap, but Phase 56 proves they
are not needed to source the “YOUR LIFE IS THE SUM...” passage: that text is
the already-solved Phase 3.2.1 Beaufort plaintext. The unresolved bridge from
that custom text to the real Architect screenplay's `choice` boundary remains
community interpretation, not a creator-authored operation.

Phase 58 nevertheless strengthens the numerical checkpoint itself. The full
Architect scene has exactly nine spoken `Matrix` occurrences; its unique
numerical Matrix context is `23 individuals / 16 female / 7 male`. The
creator-authored Phase 3.2.1 parody preserves those as `23 ciphers /
16 encryptions / 7 passwords`, while the first-piece prime matrix separately
produces `[23,16,7]`. Treat this list as intended carried-forward data. Neither
the sum `46` nor a downstream consumer is creator-specified.

Phase 61 narrows how that data is used. Telegram message `53997` is
community-authored, not a creator clue, but its claim is exactly reproducible:
splitting the recovered guide's final `be` into token `83=b, 84=e` leaves 23
guide endpoints partitioned as 16 blue and 7 yellow. This mirrors the
Architect's `23 = 16 + 7` population structure. The list is therefore best
treated as a structural selector checkpoint, not as a license for sum, XOR,
concatenation, BIP38, or another arithmetic transform. No audited operation
proposal received a creator reply.

> **Historical Phase-47 model, superseded by Phases 48-50:** the following
> two paragraphs preserve the old reasoning but are not the current verdict.

The later community `yellow-blue-primes` claim now has one unusually tight
but still incomplete convergence with that creator-grounded chain: removing
the FEFE-addressed yellow object 21 and encoding the remaining first-piece
colors as `B -> b`, `Y -> be` yields the same `23 b + 8 e` composition as the
unique all-`b/e` mask capable of producing Denis's exact extraction. The two
streams differ only by exchanging original color objects 22 and 23; the exact
conditioned family-wise base rate is `90/490,314 = 0.000183556`. Because that
exchange was noticed post hoc and lacks creator support, this remains a
near-reconstruction and does not justify cipher escalation. Recovering Denis's
missing guide image is now useful evidence work alongside the book's missing
pages 57-58.

The chain's "consume the marked prime/zero object" step had two readings
that were developed independently and never cross-checked: deleting the
addressed character from the decoded text (giving "the seed is Fe-plated")
versus removing the addressed object's color from the 24-item sequence (the
`yellow-blue-primes` convergence above). Both derive their index from the
same FEFE-address field, and removing that single object once reproduces
both results simultaneously -- they are two readouts of one operation, not
competing interpretations. See `tools/gsmg/consume_reconciliation_audit.py`.
This does not resolve the object-22/23 discrepancy above.
